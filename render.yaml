services:
  - type: web
    name: vanguard-md
    runtime: node
    plan: free
    buildCommand: npm install --legacy-peer-deps
    startCommand: node index.js
    envVars:
      - key: SESSION_ID
        sync: false
      - key: NODE_ENV
        value: production
